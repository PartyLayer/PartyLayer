# Dependencies
node_modules/
.pnp
.pnp.js

# Build outputs
dist/
build/
.next/
.turbo/
*.tsbuildinfo

# Environment — ignore ALL .env variants (secrets like the Walley devnet
# recovery phrase live here); keep only the committed .example templates.
.env
.env.*
!.env.example
!.env.*.example

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Testing
coverage/
.nyc_output/
playwright-report/
test-results/

# Temporary
tmp/
temp/
.cache/

# Package manager
.pnpm-store/

# Verification artifacts
artifacts/
fixtures/registry/tampered/
fixtures/registry/downgrade/

# Internal documentation (not for public)
PROJECT_OVERVIEW.md
CHATGPT_BRIEF.md
docs/PARTYLAYER-PRODUCT-OVERVIEW.md
MOBILE_INTEGRATION_NOTES.md

# Internal infrastructure
apps/metrics-backend/

# AI assistant config
.claude/
.vercel/

# Per-package LICENSE files copied from the root LICENSE at publish time by
# scripts/copy-license.mjs (wired into the release script). Build-time artifacts that
# make each tarball ship the license; not tracked sources.
/packages/*/LICENSE
/packages/adapters/*/LICENSE

# Test-only Walley E2E harness build artifact
apps/demo/e2e/walley-harness/bundle.js
# Test-only: branch registry copied into the harness servedir by serve.mjs
apps/demo/e2e/walley-harness/registry/

# ─── Private keys. Never in this repository, at all. ────────────────────────
# This repository is PUBLIC. The registry signing public key, the .sig files,
# sign.ts, verify.ts and SIGNING.md all belong here, because that is what makes
# verification possible by anyone. The PRIVATE key must never be here and never
# reach CI.
#
# This block is first-line defence, not the only one: a private key that lands
# in public git history is compromised permanently, and deleting it later does
# not undo that. `pnpm gate:no-private-keys` fails the build if a key-shaped
# file is present in the tree, and scripts/registry/sign.ts refuses to write a
# private key anywhere inside the working tree.
#
# `.pub` is deliberately NOT ignored: the public key is meant to be committed.
*.key
*.pem
*.p8
*.p12
*.pfx
id_rsa
id_ed25519
id_ecdsa
registry/keys/*
!registry/keys/*.pub
!registry/keys/README.md
