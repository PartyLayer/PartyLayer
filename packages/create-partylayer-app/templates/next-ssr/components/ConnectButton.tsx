'use client';

import { ConnectButton as PartyLayerConnectButton } from '@partylayer/react';

/** Client island: the full connect flow (modal → connected state → disconnect). */
export function ConnectButton() {
  return <PartyLayerConnectButton />;
}
